name: V6.4 Dual-Head Pareto Development
on:
  push:
    branches: [research-ai-market-reasoning-v6-4-dual-head-pareto-20260918]
    paths:
      - 'scripts/research_v6_4_dual_head_pareto.py'
      - '.github/workflows/research_ai_market_reasoning_v6_4_dual_head_pareto.yml'
  workflow_dispatch:
permissions:
  contents: read
  actions: read
jobs:
  year:
    strategy:
      fail-fast: false
      matrix:
        year: [2022, 2023, 2024]
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install runtime
        run: python -m pip install pandas==2.2.3 numpy==2.3.5 scipy==1.18.1 scikit-learn==1.5.2 pyarrow==18.1.0
      - name: Download exact frozen Stage0F
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api repos/${GITHUB_REPOSITORY}/actions/artifacts/10537510796/zip > /tmp/v6-0f.zip
          mkdir -p /tmp/v6-0f && unzip -q /tmp/v6-0f.zip -d /tmp/v6-0f
          mkdir -p data/history/v6-layered
          cp -a /tmp/v6-0f/. data/history/v6-layered/
      - name: Run V6.4 dual-head Pareto development
        run: python -u scripts/research_v6_4_dual_head_pareto.py --year ${{ matrix.year }}
      - name: Upload year
        uses: actions/upload-artifact@v4
        with:
          name: v6-4-dual-head-${{ matrix.year }}
          path: v6_4_dual_head_pareto_results/
          if-no-files-found: error
          retention-days: 90
  aggregate:
    needs: year
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install runtime
        run: python -m pip install pandas==2.2.3 numpy==2.3.5 scipy==1.18.1 scikit-learn==1.5.2 pyarrow==18.1.0
      - name: Download years
        uses: actions/download-artifact@v4
        with:
          pattern: v6-4-dual-head-*
          path: /tmp/v6-4
          merge-multiple: true
      - name: Aggregate
        run: python -u scripts/research_v6_4_dual_head_pareto.py --aggregate-dir /tmp/v6-4
      - name: Upload aggregate
        uses: actions/upload-artifact@v4
        with:
          name: v6-4-dual-head-development-aggregate
          path: v6_4_dual_head_pareto_results/
          if-no-files-found: error
          retention-days: 90
