name: Adaptive Router Stage14 Relative Strength Dual Gate
on:
  push:
    branches: [r10-stage14-relative-strength-dualgate-20260910]
    paths:
      - 'scripts/adaptive_router_stage14_relative_strength_dualgate.py'
      - '.github/workflows/adaptive_router_stage14.yml'
  workflow_dispatch:
permissions:
  actions: read
  contents: read
jobs:
  stage14:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas numpy pyarrow
      - name: Restore cached market bundle
        uses: actions/download-artifact@v4
        with:
          name: R10-2016-2020-reference-data-complete
          path: reference_bundle
          github-token: ${{ github.token }}
          repository: xchixmeng-svg/alphapilot-market-data-v2
          run-id: 34313660140
      - name: Run Stage14
        run: python scripts/adaptive_router_stage14_relative_strength_dualgate.py
      - name: Guard research contract
        run: |
          python - <<'PY'
          import json
          m=json.load(open('adaptive_router_stage14/manifest.json'))
          assert m['market_refetch'] is False and m['formal_r10_modified'] is False
          assert m['uses_2021_2025'] is False and m['future_labels_used'] is False
          assert m['benchmark_gate_required'] is True and m['benchmark']=='0050'
          assert m['oos1_used_for_selection'] is False and m['oos2_used_for_selection'] is False
          assert m['t_plus_1'] is True and m['integer_shares'] is True and m['shared_cash'] is True
          print('STAGE14_GATES_PASS')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: adaptive-router-stage14
          path: adaptive_router_stage14/
          if-no-files-found: error